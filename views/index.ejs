<%- include("partials/header") %>
<section class="hero-banner" aria-label="Banner principal">
  <div class="hero-banner-content">
    <h1>Reservas e Pagamentos Online para Salao</h1>
    <p>Agende servicos, pague por M-Pesa/e-Mola/cartao e acompanhe tudo em tempo real.</p>
    <p><strong>Rapido. Organizado. Lucrativo.</strong></p>
  </div>
  <div class="hero-banner-actions">
    <a class="btn" href="/login">Criar uma reserva</a>
  </div>
</section>

<section class="home-metrics">
  <article class="card metric-card metric-card-growth">
    <p class="metric-icon"><i class="bi bi-graph-up-arrow" aria-hidden="true"></i></p>
    <h3>+40%</h3>
    <p>Melhoria esperada na organizacao da agenda.</p>
  </article>
  <article class="card metric-card metric-card-availability">
    <p class="metric-icon"><i class="bi bi-phone" aria-hidden="true"></i></p>
    <h3>24/7</h3>
    <p>Clientes reservam a qualquer hora pelo celular.</p>
  </article>
  <article class="card metric-card metric-card-realtime">
    <p class="metric-icon"><i class="bi bi-lightning-charge" aria-hidden="true"></i></p>
    <h3>Tempo Real</h3>
    <p>Acompanhamento de reservas e receitas no dashboard.</p>
  </article>
</section>

<section class="services-ticker-section">
  <h2>Servicos em destaque</h2>
  <div class="services-ticker-viewport">
    <div class="services-ticker-track">
      <% services.forEach(service => { %>
      <article class="card service-highlight ticker-card">
        <img src="<%= service.image %>" alt="<%= service.name %>" class="ticker-thumb" />
        <div class="ticker-card-body">
          <h3><%= service.name %></h3>
          <p><%= service.duration %> min</p>
          <strong><%= service.price %> MT</strong>
        </div>
      </article>
      <% }) %>
      <% services.forEach(service => { %>
      <article class="card service-highlight ticker-card" aria-hidden="true">
        <img src="<%= service.image %>" alt="" class="ticker-thumb" />
        <div class="ticker-card-body">
          <h3><%= service.name %></h3>
          <p><%= service.duration %> min</p>
          <strong><%= service.price %> MT</strong>
        </div>
      </article>
      <% }) %>
    </div>
  </div>
</section>

<%- include("partials/footer") %>
